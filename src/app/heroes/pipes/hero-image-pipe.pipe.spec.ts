import { HeroImagePipePipe } from './hero-image.pipe';

describe('HeroImagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new HeroImagePipePipe();
    expect(pipe).toBeTruthy();
  });
});
